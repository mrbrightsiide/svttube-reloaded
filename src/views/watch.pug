extends base

block content
  #videoContainer(data-id=video._id)
    video(src='/' + video.fileUrl)
    #videoControls.videoControls
      .videoControls__play
        span#play.videoControls__playBtn
          i.fas.fa-play
        .videoControls__time
          span#currenTime 00:00
          span /
          span#totalTime 00:00
      input#timeline.videoControls__timeline(
        type="range",
        step="1",
        value="0",
        min="0"
      )
      .videoControls__volume
        input#volume(type="range", step="0.01", value=0.5, min="0", max="1")
        span#mute
          i.fas.fa-volume-up
      div
        span#fullScreen
          i.fas.fa-expand
    .video__data
        p.vidieo__title=video.title
        small.video__owner Uploaded by
          a(href=`/users/${video.owner._id}`)=video.owner.name
        small.video__createdAt=new Date(video.createdAt).toLocaleDateString("ko-kr", {weekday:'long', year: 'numeric', month:'long', day:'numeric'})
        if String(video.owner._id) === String(loggedInUser._id)
          a(href=`${video.id}/edit`) Edit Video &rarr;
          br
          a(href=`${video.id}/delete`) Delete Video &rarr;
    if loggedIn
      .video__comments
        form.video__commnt-form#commentForm
          textarea(cols="30", rows="10", placeholder="Leave a comment here")
          button Add Comment
        
block scripts
  script(src="/static/js/videoPlayer.js")
  if loggedIn 
    script(src="/static/js/commentSection.js") 
